# V55 — Day / Night Mode

- Added persistent Day Mode / Night Mode toggle in the hamburger menu.
- Theme preference is stored in localStorage and survives reloads.
- Dark theme styling added for major cards, forms, navigation, quizzes/exams, batch pages, and admin UI.
- Theme color meta updates with the selected mode.
- Service worker cache bumped to v18 and includes assets/theme.js.
- No Supabase SQL changes required.
